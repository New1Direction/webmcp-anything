{
  "name": "webmcp-anything-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted API for WebMCP Anything — turn any URL into a WebMCP tool schema.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "marked": "^18.0.4",
    "typescript": "^5.6.0",
    "wrangler": "^3.99.0"
  }
}
